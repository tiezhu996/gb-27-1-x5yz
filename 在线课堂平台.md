## 项目主要需求

### 项目概述

构建一个支持直播授课与录播回放的在线课堂平台，面向 K12 及职业培训机构，教师可创建课程、进行实时直播教学，学生可在线学习、参与课堂互动并完成课后作业。平台需支持多人实时音视频互动和电子白板功能。

### 功能需求

1. **用户管理与角色系统**：支持学生、教师、管理员三种角色注册登录，教师需提交资质认证申请，管理员审核通过后方可开课；学生支持微信扫码登录和个人手机号注册
2. **课程管理**：教师可创建课程（含课程名称、封面、简介、定价、课时列表），支持免费课和付费课两种模式，课程支持按学科分类和标签检索
3. **直播授课**：教师发起实时直播课堂，支持屏幕共享、电子白板标注、课件展示，学生端支持举手发言、文字弹幕互动，单间课堂支持最多 200 人同时在线
4. **录播回放**：直播结束后自动生成录播视频，学生可在课程详情页回看任意课时的录播，支持倍速播放（0.5x-2.0x）和进度条拖拽
5. **课堂签到与考勤**：教师在直播过程中可发起签到，学生在规定时间内完成签到，系统自动生成考勤报表，支持导出 Excel
6. **作业系统**：教师为每个课时布置作业（支持文本题、选择题、附件提交），学生在线提交，教师批改后给出评分和文字反馈
7. **学习数据统计**：为教师和学生分别提供数据看板——教师可查看课程销量、学生出勤率、作业完成率；学生可查看学习时长、课程进度、成绩趋势

### 技术要求

- 前端：React + TypeScript，使用 Ant Design 组件库，Vite 构建工具
- 后端：NestJS 框架，TypeScript 开发
- 数据库：PostgreSQL，使用 TypeORM 做数据持久化
- 缓存：Redis 用于会话管理和课堂在线状态
- 实时通信：WebSocket 用于课堂弹幕和举手互动
- 文件存储：课程视频和课件上传至 MinIO 对象存储
- 视频处理：使用 FFmpeg 进行直播录制和转码
- 认证方式：JWT + 微信 OAuth2.0

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 暴露端口（避免与其他项目冲突）：前端 8001，后端 3001，PostgreSQL 5501，Redis 6401，MinIO 9001/9002
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

